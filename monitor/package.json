{
  "name": "@aiready/landing-monitor",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "format-check": "prettier --check . --ignore-path ../../.prettierignore"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
